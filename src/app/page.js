'use client'
import { Button } from '@mui/material';
import * as React from 'react';
let z = 'spdk dok sdvm sdmvo sdm dfvm '

export default function ButtonUsage() {
  return <Button variant="outlined"  color="secondary" onClick={()=>{console.log(1+1)}}>Hello world</Button>;
}
 